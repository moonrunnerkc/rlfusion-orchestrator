# backend/requirements.txt
# Author: Bradley R. Kinnard
# GPU-first: RTX 50-series Blackwell (cu128). CPU fallback safe.

# PyTorch CUDA 12.8 index, must appear before torch pins.
--extra-index-url https://download.pytorch.org/whl/cu128

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.9
slowapi==0.1.9             # rate limiting

# PyTorch (Blackwell cu128)
torch>=2.9
torchaudio>=2.9
torchvision>=0.24

# LLM inference (asymmetric dual-model pipeline)
# llama-cpp-python requires CUDA build for Blackwell:
#   pip install llama-cpp-python --force-reinstall --no-cache-dir \
#     -C cmake.args="-DGGML_CUDA=on -DCMAKE_CUDA_ARCHITECTURES=100;120"
llama-cpp-python>=0.3.0,<1
json-repair>=0.30,<1        # robust JSON parsing for CPU triage output
huggingface-hub>=0.25,<1   # model artifact download + management
pynvml>=12.0,<13           # VRAM/GPU monitoring

# Embeddings & NLP
sentence-transformers==3.1.1
tiktoken>=0.7.0            # token counting in CSWR (optional import, but should be present)

# RL stack
stable-baselines3==2.3.2
gymnasium==0.29.1
d3rlpy>=2.6.0              # offline CQL training

# LoRA fine-tuning (POST /api/fine-tune): pulls transformers + trl + datasets.
peft>=0.13,<1
transformers>=4.47,<5
trl>=0.12,<1
datasets>=3.2,<4

# Core deps
numpy>=1.26.4
cloudpickle>=3.0.0
networkx>=3.3
PyYAML==6.0.2

# Multi-agent orchestration
langgraph>=0.2,<1
ollama==0.3.3
tqdm==4.66.5
python-dotenv==1.0.1
typing_extensions>=4.12.2
PyPDF2==3.0.1              # PDF text extraction at index time
scipy>=1.13.1
scikit-learn>=1.5.2        # Ledoit-Wolf OOD detector

# GraphRAG / hybrid retrieval
qdrant-client>=1.12,<2     # in-memory entity vector + payload search
leidenalg>=0.10,<1         # Leiden community detection
igraph>=0.11,<1            # graph backend for leidenalg

# HTTP / web
httpx==0.27.2
beautifulsoup4==4.12.3

# Monitoring
prometheus-client>=0.21,<1 # Prometheus metrics endpoint

# Dev / test (only needed when running test suites)
psutil>=5.9                # sim_env.py GPU/memory stats
locust>=2.31               # load testing
tensorboard>=2.18          # training viz
